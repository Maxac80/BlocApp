rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORAR - pentru testare inițială
    // Permite oricărui utilizator autentificat să facă orice
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}